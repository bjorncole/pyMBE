from ..label import *

def build_stable_id_lookup(all_elements: dict) -> dict:
    """
    Builds a dictionary that has a key based on a user-stable element attribute like qualified name, mapped to
    server ID
    :return: dict of stable signature -> unique identifier
    """

    return {}

def id_to_stable_name_lookup(all_elements: dict) -> dict:
    """
    Builds a dictionary that has the server ID as a key but calculates a user-stable / user-readable signature
    :param all_elements:
    :return: dict of unique identifier -> stable signature
    """

    return {}