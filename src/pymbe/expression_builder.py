

def build_expression_tree():
    """
    Unlike other elements, the nodes on an expression tree are not particularly good and standing
    by themselves while other parts of the tree are being built. Rather than build the tree one-by-one
    from model elements, it is better to use the dictionary representation of the expression nodes and
    edges until the expression is complete and ready to be converted into elements when it is self-consistent.
    """

    # Other methods are needed here to create the dictionary elements for the major relationship types and
    # nodes and then glue them together into this tree.

    pass